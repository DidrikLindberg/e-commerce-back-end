jobs:
  visualize:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Generate codebase visualization
      uses: githubocto/repo-visualizer@v0.7.0
      with:
        args: >
          --output-format=svg
          --output-filename=diagram.svg
